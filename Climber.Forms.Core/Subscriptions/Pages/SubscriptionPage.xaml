<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:material="clr-namespace:XF.Material.Forms.UI;assembly=XF.Material"
    x:Class="Climber.Forms.Core.SubscriptionPage"
	
	Title="{Binding Title}">
	<ContentPage.ToolbarItems>
		<ToolbarItem Name="Add" Command="{Binding CommandAddSubscription}" Order="Primary" Priority="0" />
	</ContentPage.ToolbarItems>
	<CollectionView ItemsSource="{Binding Sessions}" Margin="0,10,0,10">
		<CollectionView.ItemTemplate>
			<DataTemplate>
				<ContentView>
					<material:MaterialCard CornerRadius="10"  Margin="20,5,20,5" Elevation="1" IsClickable="True">
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="10" />
							</Grid.ColumnDefinitions>
							<Grid.RowDefinitions>
								<RowDefinition Height="1*" />
								<RowDefinition Height="1*" />
							</Grid.RowDefinitions>
							<Label Text="{Binding LblType}" Style="{DynamicResource ListItemDetailTextStyle}" />
							<Label Text="{Binding LblDate}"
								   Grid.Row="1"/>
							<Label Text="A"
								   IsVisible="{Binding IsActive}"
								   Grid.Column="1"/>
						</Grid>
					</material:MaterialCard>
				</ContentView>
			</DataTemplate>
		</CollectionView.ItemTemplate>
	</CollectionView>
</ContentPage>
