<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:material="clr-namespace:XF.Material.Forms.UI;assembly=XF.Material"
    x:Class="Climber.Forms.Core.ClimbingClubOverviewPage"
	Title="{Binding Title}">
	<ContentPage.ToolbarItems>
		<ToolbarItem Name="Add" Command="{Binding CommandAddClub}" Order="Primary" Priority="0" />
	</ContentPage.ToolbarItems>
	<CollectionView ItemsSource="{Binding Clubs}" Margin="0,10,0,10">
		<CollectionView.ItemTemplate>
			<DataTemplate>
				<ContentView>
					<material:MaterialCard CornerRadius="10"
										   Margin="20,5,20,5"
										   Elevation="1"
										   IsClickable="True"
										   ClickCommand="{Binding CommandClicked}">
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="15" />
							</Grid.ColumnDefinitions>
							<Grid.RowDefinitions>
								<RowDefinition Height="1*" />
							</Grid.RowDefinitions>
							<Label Text="{Binding Name}" Style="{DynamicResource ListItemDetailTextStyle}" />
							<Label Text="M"
								   IsVisible="{Binding IsMember}"
								   Grid.Column="1"/>
						</Grid>
					</material:MaterialCard>
				</ContentView>
			</DataTemplate>
		</CollectionView.ItemTemplate>
	</CollectionView>
</ContentPage>
